# API host used at build-time by `import.meta.env.VITE_API_HOST` (see src/config.ts).
# CloudFront serves both the SPA and /api/* on the same origin, so leaving this
# empty makes the SPA call the same domain it was loaded from.
VITE_API_HOST=
